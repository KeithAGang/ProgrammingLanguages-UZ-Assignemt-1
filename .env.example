# Database Configuration
# SQLite database file location
# Default: file:local.db (used automatically if DATABASE_URL is not set)
# You can copy this file to .env or leave it unset to use the default
DATABASE_URL=file:local.db
